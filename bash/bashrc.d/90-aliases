# -*- mode: shell-script -*-

alias reload='. ~/.bash_profile'

# Do you like a colorful grep? I do!
alias grep="grep --color=auto"

function volume {
  arch -i386 osascript -e "set volume output volume $1"
}

alias xmlcat="tidy -i -xml"

alias unfuck-paste='printf "\e[?2004l"'
